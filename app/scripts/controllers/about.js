'use strict';

/**
 * @ngdoc function
 * @name vmSanityCheckApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the vmSanityCheckApp
 */
angular.module('vmSanityCheckApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
