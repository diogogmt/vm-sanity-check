'use strict';

/**
 * @ngdoc function
 * @name vmSanityCheckApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the vmSanityCheckApp
 */
angular.module('vmSanityCheckApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
